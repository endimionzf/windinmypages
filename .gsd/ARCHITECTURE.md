# Architecture

> Auto-generated by /map on 2026-04-02

## Overview

"Wind in my Pages" is a cinematic web-based art portfolio. It features a high-impact, state-driven introduction sequence followed by an interactive horizontal/vertical gallery of artwork.

```
┌─────────────────────────────────────────┐
│              [index.html]               │
│         (Structure & Entry Point)       │
├─────────────────────────────────────────┤
│              [js/script.js]             │
│        (State Engine & Event Bus)       │
├─────────────────────────────────────────┤
│            [css/style.css]              │
│       (Animations & Visual Layer)       │
└─────────────────────────────────────────┘
```

## Components

### Intro Sequence (`#intro-scroll`)
- **Purpose:** Immersive storytelling through staged overlays.
- **Location:** `index.html`, `js/script.js`
- **States:** 12 specific stages (0-11 for intro, 12 for gallery entry).

### Background Media (`#sticky-frame`)
- **Purpose:** Responsive cinematic background.
- **Implementation:**
  - **Desktop:** Autoplay video background (`wimp-fullscreen.mp4`).
  - **Mobile/Tablet:** Ken Burns animated image slider (`slides/optimized/`).

### Gallery (`#container`)
- **Purpose:** Display artwork metadata and high-res images.
- **Implementation:** Dynamically populated from `artworks` array in `script.js`.

## Data Flow

1. **Initialization:** `initExperience()` detects device width and starts video or slider.
2. **User Interaction:** Scroll (wheel) or Touch (swipe) events are captured.
3. **State Transition:** `updateState()` modifies `currentState` and calls `renderState()`.
4. **Visual Update:** `renderState()` toggles CSS classes (opacity, display) for overlays.
5. **Session Transition:** `enterGallery()` hides the intro and reveals the gallery.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Google Fonts | CDN | Typography (Lexend, Playfair Display) |

## Technical Debt

- [ ] **Hardcoded Content:** Artwork data is stored directly in `script.js`.
- [ ] **State Logic:** Magic numbers for scroll thresholds and animation timings.
- [ ] **Responsiveness:** Device detection is based on a fixed 1366px width breakpoint.
- [ ] **Media Optimization:** Direct inclusion of MP4 and PNGs without lazy-loading or modern formats (WebP/AVIF).

## Conventions

**Naming:** Kebab-case for CSS classes; CamelCase for JavaScript functions/variables.
**Structure:** Traditional flat directory structure (`css/`, `js/`, `images/`).
**Testing:** No automated testing framework present.
